services:
  - type: web
    name: templo-aiden-lucian
    env: python
    buildCommand: pip install -r requisitos.txt
    startCommand: uvicorn Expansión_Segura_Aiden:app --host 0.0.0.0 --port 10000
    plan: free
    envVars:
      - key: OPENAI_API_KEY
        sync: false
